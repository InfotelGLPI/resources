{
    "config": {
        "platform": {
            "ext-ldap": "8.0"
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "require-dev": {
        "glpi-project/tools": "^0.8"
    },
    "require": {
        "php": ">=8.2",
        "adldap2/adldap2": "^10.5"
    }
}
